[package]
name = "fireshark-core"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
fireshark-dissectors = { path = "../fireshark-dissectors" }
fireshark-file = { path = "../fireshark-file" }
